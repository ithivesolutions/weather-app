{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "weather application to get the https request and response from the third party website to get the data from the same",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0"
  }
}
